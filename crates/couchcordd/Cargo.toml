[package]
name = "couchcordd"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "couchcord daemon — Phase 0: the `doctor` de-risk gate"

[dependencies]
anyhow = "1"
evdev = "0.12"
x11rb = "0.13"
