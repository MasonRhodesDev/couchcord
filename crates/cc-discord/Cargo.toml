[package]
name = "cc-discord"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "couchcord Discord local-RPC: framing, parse, voice filter, command building (pure); socket IO is Phase 2"

[dependencies]
cc-core = { path = "../cc-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
