[package]
publish = false
name = "cc-input"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "couchcord input: Steam virtual-kbd classification + keymap (pure) + evdev InputSource"

[dependencies]
cc-core = { path = "../cc-core" }
evdev = "0.12"
libc = "0.2"
tokio = { version = "1", features = ["sync", "rt"] }
futures-core = "0.3"
futures-util = "0.3"
tracing = "0.1"
